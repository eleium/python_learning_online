"""
python中，函数与方法有啥区别？

好问题！让我详细解释一下**函数与方法的区别**：

## 核心区别：

### 1. **调用方式不同**
- **函数**：独立存在，直接调用
  ```python
  sorted(s)      # ✓ 函数，直接用
  len(s)         # ✓ 函数
  max(s)         # ✓ 函数
  ```


- **方法**：属于某个对象，必须通过对象调用
  ```python
  s.sort()       # ✓ 方法，需要 s. 来调用
  name.upper()   # ✓ 字符串的方法
  list.append()  # ✓ 列表的方法
  ```


### 2. **归属关系不同**
- **函数**：不属于任何特定的对象，是独立的
  - `sorted()`、`len()`、`max()`、`min()` 等
  - 可以作用于多种类型（列表、元组、字符串等）

- **方法**：是对象的一部分，只能被特定类型使用
  - `list.sort()` 只能用于列表
  - `str.upper()` 只能用于字符串
  - `list.append()` 只能用于列表

### 3. **语法格式不同**
```python
# 函数的格式：函数名 (参数)
result = sorted(s)        # 函数
length = len(s)           # 函数

# 方法的格式：对象。方法名 (参数)
s.sort()                  # 方法，注意有个点 .
name.capitalize()         # 方法
```


### 4. **实际例子对比**

```python
s = [3, 1, 2]

# sorted() 是函数
result1 = sorted(s)    # 函数调用
print(result1)         # [1, 2, 3]
print(s)               # [3, 1, 2] ← 原列表不变

# sort() 是方法
result2 = s.sort()     # 方法调用（通过 s.）
print(result2)         # None
print(s)               # [1, 2, 3] ← 原列表被修改
```


## 你的代码中的例子：

```python
# 第 82 行：
sorted(s)    # 这是函数，不属于任何对象

# 第 70 行：
w.sort()     # 这是方法，属于列表 w 的
```


## 简单记忆法：
- **看有没有点号 `.`**
  - 有 `.` → 方法（如 `s.sort()`）
  - 没有 `.` → 函数（如 `sorted(s)`）

- **看是谁在"行动"**
  - 函数：主动对数据操作 → `sorted(s)`（sorted 主动排序 s）
  - 方法：对象自己行动 → `s.sort()`（s 自己排序）

## 为什么要区分？
因为它们的**行为特性**不同：
- 函数通常返回新对象（如 `sorted()` 返回新列表）
- 方法可能修改原对象（如 `sort()` 修改原列表）

"""