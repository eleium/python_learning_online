"""
有了印象的
def tempurature_convertion(c):
    f=c*1.8+32
    return f

f=tempurature_convertion(5)
print(f)
c=float(input('c:'))
print(c)
定义函数，调用函数

class MyClass:
    def __init__(self,a,b,c):
        self.a=a
        self.b=b
        self.c=c
定义类，类里面有函数
class MyClass:
    def __init__(self,a,b,c):
        self.a=a
        self.b=b

import math
from math import math
引用导入
class MyClass:
    def __init__(self,a,b,c):
        self.a=a
        self.b=b


"""


class myclass:

    def __init__(self, a, b, c):#构造myclass的属性有三：a,b,c
        self.a = a
        self.b = b
        self.c = c
#没有创建实例，所以这个类没啥用。下面的跟这个类没关系。

        def sum(self):
            sum=self.a+self.b+self.c
xuanxuan=myclass(7,8,9)
print(xuanxuan)

# i=
# a=int(input('a:'))
# b=int(input('b:'))
# c=int(input('c:'))
# x=a+b+c
# print(x)

#流程图是线性的，而人的思维是发散的。所以可以用思维导图来呈现。Maid Map.思维导图又叫心智图。
# 是表达发散性思维的有效的图形思维工具。简单又有效。一种革命性的思维工具。