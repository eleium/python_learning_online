 python 的计算：
+
-
*
/
// 求商(结果为浮点数）舍弃小数部分
% 求模（除后剩下的余数）
-x 相反数
+x  x 的本身
abs(x) x的绝对值
int(x)  整数
float(x) 浮点数
complex(re,im) 返回一个复数，re实部，im虚部，二者都是浮点数
复数如何计算？是实部相计算，虚部相计算吗？是的。有啥用？

x ** y 计算x的y次方
pow(x,y)#pow() 独有的功能，第三个参数 z 可选，作用是计算 x 的 y 次方后对 z 取模，即 (x ** y) % z。